// on crée le tableau des codes des agents
var tabCodespy = new Array();

// on crée pour chaque code, les noms correspondants sous forme de tableau
for (var i = 0; i < 9; i++) {
  tabNomspy[i] = new Array();
}
